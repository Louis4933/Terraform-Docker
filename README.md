# Deploying a Docker container from Terraform

### To deploy the infrastructure, in a terminal :

cd terraform-docker/

./deploy_infra.sh

### Application available on internal port 8000

### To destroy it :

./destroy_infra.sh
